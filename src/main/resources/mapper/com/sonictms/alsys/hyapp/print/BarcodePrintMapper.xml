<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sonictms.alsys.hyapp.print.mapper.BarcodePrintMapper">

    <select id="selectPendingList" resultType="PrintQueueVO">
        SELECT TOP 10 SEQ, AGNT_NM, MTRL_NM, MTRL_CD, BARCODE, QTY, REQ_DT
        FROM TBL_PRINT_QUEUE
        WHERE STATUS = '0'
        ORDER BY REQ_DT ASC
    </select>

    <select id="selectPrintInfoBySeq" resultType="PrintQueueVO">
        SELECT SEQ, AGNT_NM, MTRL_NM, MTRL_CD, BARCODE, QTY
        FROM TBL_PRINT_QUEUE
        WHERE SEQ = #{seq}
    </select>

    <update id="updatePrintStatus">
        UPDATE TBL_PRINT_QUEUE
        SET STATUS = #{status}, DONE_DT = GETDATE(), ERR_MSG = #{errMsg}
        WHERE SEQ = #{seq}
    </update>

</mapper>
